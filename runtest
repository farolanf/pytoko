if [ -z $1 ]; then
    TEST=toko.tests
else
    TEST=$1
fi

watch "coverage run --source='.' manage.py test $TEST && coverage report" pytoko/ toko/ --wait=10 --interval=3 --ignoreDirectoryPattern /screenshots/